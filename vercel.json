{
  "version": 2,
  "installCommand": "npm install --ignore-scripts --prefix apps/frontend",
  "buildCommand": "cd apps/frontend && npm run build",
  "outputDirectory": "apps/frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
